services:
  builder:
    build: .
    image: ztracker-builder
    tty: true
    stdin_open: true
    volumes:
      # Mount source code but let the container use its own node_modules
      - .:/app
    environment:
      - NODE_ENV=development
